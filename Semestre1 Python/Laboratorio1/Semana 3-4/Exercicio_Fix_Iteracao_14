numero = int(input("Digite um número: "))

if numero < 2:
    print("Não é primo.")
else:
    primo = True
    contador = 2

    while contador < numero:
        if numero % contador == 0:
            primo = False
            print("Número não é primo.")
            break
        contador += 1

    if primo:
        print("Número primo.")